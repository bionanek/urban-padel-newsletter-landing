---
---
<div class="slash-div"></div>

<style>
  .slash-div {
    height: 40px;
    background: repeating-linear-gradient(
      -50deg,
      var(--bg) 0 20px,
      var(--acid) 20px 22px,
      var(--bg) 22px 42px,
      var(--magenta) 42px 44px
    );
  }
</style>
